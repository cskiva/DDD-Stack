{
	"private": true,
	"scripts": {
		"dev:services": "docker compose -f docker-compose.yml up -d",
		"dev:next": "bash -lc 'cd nextjs-app && npm run dev'",
		"dev:strapi": "bash -lc 'cd strapi/strapi-app && npm run dev",
		"dev": "npm run dev:services && npm run dev:next && npm run dev:strapi",
		"stop": "docker compose down",
		"clean": "docker compose down -v --remove-orphans"
	},
	"devDependencies": {
		"cross-env": "^7.0.3"
	}
}